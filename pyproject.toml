[project]
name = "jamie"
version = "0.1.0"
description = "Prints labels from the command line."
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "Pillow",
]

[project.scripts]
jamie = "jamie:main"